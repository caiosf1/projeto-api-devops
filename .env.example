# ===================================================================================
# Configurações da Aplicação Flask
# ===================================================================================

# Chave secreta para sessões Flask (gere uma chave única e segura)
SECRET_KEY=your-secret-key-here

# Chave secreta para JWT (gere outra chave única e diferente)
JWT_SECRET_KEY=your-jwt-secret-key-here

# Ambiente de execução (development, production, testing)
FLASK_ENV=development

# ===================================================================================
# Configurações do Banco de Dados PostgreSQL
# ===================================================================================

# 🎯 DESENVOLVIMENTO LOCAL (SQLite)
# Deixe DATABASE_URL vazio para usar SQLite automaticamente
# DATABASE_URL=

# 🐳 DOCKER COMPOSE (PostgreSQL local)
# Descomente para usar com docker-compose.yml
# POSTGRES_USER=your_username
# POSTGRES_PASSWORD=your_password
# POSTGRES_DB=your_database
# POSTGRES_SERVER=db
# POSTGRES_PORT=5432

# ☁️ PRODUÇÃO (Azure Database for PostgreSQL)
# Configure no Azure Portal ou GitHub Secrets
# POSTGRES_SERVER=your-server.postgres.database.azure.com
# POSTGRES_USER=your_username
# POSTGRES_PASSWORD=your_strong_password
# POSTGRES_DB=your_database
# POSTGRES_PORT=5432
# POSTGRES_SSL_MODE=require

# ===================================================================================
# Configurações do Docker Hub (para CI/CD)
# ===================================================================================

# Seu username do Docker Hub
DOCKERHUB_USERNAME=your-dockerhub-username

# Token de acesso do Docker Hub (configure no GitHub Secrets)
# DOCKERHUB_TOKEN=your-token-here

# ===================================================================================
# Instruções de Uso
# ===================================================================================

# 1. Copie este arquivo para .env:
#    cp .env.example .env
#
# 2. Gere chaves secretas seguras:
#    python -c "import secrets; print(secrets.token_hex(32))"
#
# 3. Substitua os valores de SECRET_KEY e JWT_SECRET_KEY
#
# 4. Configure as credenciais do banco de dados
#
# 5. NUNCA commite o arquivo .env no Git!
#    (já está no .gitignore)

# ===================================================================================
# Notas de Segurança
# ===================================================================================

# ⚠️  IMPORTANTE:
# - Use senhas fortes em produção (mínimo 12 caracteres)
# - Nunca compartilhe suas chaves secretas
# - Use variáveis de ambiente em produção (GitHub Secrets, Azure App Settings)
# - Rotacione suas chaves periodicamente
# - Este é apenas um EXEMPLO - não use estes valores!
