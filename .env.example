# ===================================================================================
# Configurações da Aplicação Flask
# ===================================================================================

# Chave secreta para sessões Flask (gere uma chave única e segura)
SECRET_KEY=sua-chave-secreta-aqui-use-secrets.token_hex(32)

# Chave secreta para JWT (gere outra chave única e diferente)
JWT_SECRET_KEY=sua-chave-jwt-aqui-use-secrets.token_hex(32)

# Ambiente de execução (development, production, testing)
FLASK_ENV=development

# ===================================================================================
# Configurações do Banco de Dados PostgreSQL
# ===================================================================================

# Usuário do PostgreSQL
POSTGRES_USER=caio

# 🔐 Senha do PostgreSQL (OBRIGATÓRIO: USE SENHA FORTE!)
POSTGRES_PASSWORD=SuaSenhaForteAqui123!

# Nome do banco de dados
POSTGRES_DB=apitodo

# Host do banco (use 'db' para Docker Compose, 'localhost' para desenvolvimento local)
POSTGRES_HOST=db

# Porta do PostgreSQL
POSTGRES_PORT=5432

# URL completa do banco de dados (será construída automaticamente)
# Formato: postgresql://usuario:senha@host:porta/database
# DATABASE_URL=postgresql://caio:minhasenha@db:5432/apitodo

# ===================================================================================
# Configurações do Docker Hub (para CI/CD)
# ===================================================================================

# Seu username do Docker Hub
DOCKERHUB_USERNAME=seu-usuario-dockerhub

# Token de acesso do Docker Hub (NÃO compartilhe!)
# DOCKERHUB_TOKEN=seu-token-aqui

# ===================================================================================
# Instruções de Uso
# ===================================================================================

# 1. Copie este arquivo para .env:
#    cp .env.example .env
#
# 2. Gere chaves secretas seguras:
#    python -c "import secrets; print(secrets.token_hex(32))"
#
# 3. Substitua os valores de SECRET_KEY e JWT_SECRET_KEY
#
# 4. Configure as credenciais do banco de dados
#
# 5. NUNCA commite o arquivo .env no Git!
#    (já está no .gitignore)

# ===================================================================================
# Notas de Segurança
# ===================================================================================

# ⚠️  IMPORTANTE:
# - Use senhas fortes em produção
# - Nunca compartilhe suas chaves secretas
# - Use variáveis de ambiente em produção (Azure App Settings, etc.)
# - Rotacione suas chaves periodicamente
