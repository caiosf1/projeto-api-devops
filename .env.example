# ===================================================================================
# Configurações da Aplicação Flask
# ===================================================================================

# Chave secreta para sessões Flask (gere uma chave única e segura)
SECRET_KEY=sua-chave-secreta-aqui-use-secrets.token_hex(32)

# Chave secreta para JWT (gere outra chave única e diferente)
JWT_SECRET_KEY=sua-chave-jwt-aqui-use-secrets.token_hex(32)

# Ambiente de execução (development, production, testing)
FLASK_ENV=development

# ===================================================================================
# Configurações do Banco de Dados PostgreSQL
# ===================================================================================

# 🎯 DESENVOLVIMENTO LOCAL (SQLite ou PostgreSQL local)
# Deixe comentado para usar SQLite em desenvolvimento
# DATABASE_URL=sqlite:///dev.db

# � DOCKER COMPOSE (PostgreSQL em container local)
# Descomente para usar com docker-compose
# POSTGRES_USER=pgladmin
# POSTGRES_PASSWORD=SuaSenhaAqui123!
# POSTGRES_DB=apitodo
# POSTGRES_SERVER=db
# POSTGRES_PORT=5432

# ☁️ AZURE DATABASE FOR POSTGRESQL (Produção recomendada)
# Use para Azure - configure estas variáveis no Azure Portal ou CI/CD
# POSTGRES_SERVER=seu-servidor.postgres.database.azure.com
# POSTGRES_USER=pgadmin
# POSTGRES_PASSWORD=Senh@Forte123!
# POSTGRES_DB=apitodo
# POSTGRES_PORT=5432
# POSTGRES_SSL_MODE=require

# 📦 AZURE CONTAINER APPS POSTGRESQL (NÃO RECOMENDADO - apenas teste)
# Não use em produção - sem persistência garantida
# POSTGRES_SERVER=postgres-app.internal.....azurecontainerapps.io
# POSTGRES_USER=pgladmin
# POSTGRES_PASSWORD=MinhaSenh123
# POSTGRES_DB=apitodo
# POSTGRES_PORT=5432
# POSTGRES_SSL_MODE=prefer

# ===================================================================================
# Configurações do Docker Hub (para CI/CD)
# ===================================================================================

# Seu username do Docker Hub
DOCKERHUB_USERNAME=seu-usuario-dockerhub

# Token de acesso do Docker Hub (NÃO compartilhe!)
# DOCKERHUB_TOKEN=seu-token-aqui

# ===================================================================================
# Instruções de Uso
# ===================================================================================

# 1. Copie este arquivo para .env:
#    cp .env.example .env
#
# 2. Gere chaves secretas seguras:
#    python -c "import secrets; print(secrets.token_hex(32))"
#
# 3. Substitua os valores de SECRET_KEY e JWT_SECRET_KEY
#
# 4. Configure as credenciais do banco de dados
#
# 5. NUNCA commite o arquivo .env no Git!
#    (já está no .gitignore)

# ===================================================================================
# Notas de Segurança
# ===================================================================================

# ⚠️  IMPORTANTE:
# - Use senhas fortes em produção
# - Nunca compartilhe suas chaves secretas
# - Use variáveis de ambiente em produção (Azure App Settings, etc.)
# - Rotacione suas chaves periodicamente
